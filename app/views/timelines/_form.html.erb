<%= form_with(model: action, local: true) do |form| %>
  <% if action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(action.errors.count, "error") %> prohibited this timeline from being saved:</h2>

      <ul>
      <% action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
